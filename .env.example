# Copy to .env and adjust for local development (optional).
# Flask session signing — required for production deployments.
FLASK_SECRET_KEY=change-me-to-a-long-random-string

# OBS WebSocket (optional; can also be set in the in-app Settings page)
# OBS_HOST=localhost
# OBS_PORT=4455
# OBS_PASSWORD=
